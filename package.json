{
  "name": "bbcc-templet",
  "version": "1.0.0",
  "description": "Modern Education Management System with Glassmorphism UI",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js"
  },
  "dependencies": {
    "express": "^4.18.2",  // Yeh Express framework backend routes ke liye use hota hai.
    "mongoose": "^7.0.0",  // Yeh MongoDB connection aur schemas ke liye hai.
    "socket.io": "^4.7.0",  // Yeh real-time notifications (popups) ke liye hai.
    "body-parser": "^1.20.2",  // Yeh request bodies parse karne ke liye hai.
    "cors": "^2.8.5"  // Yeh cross-origin requests allow karne ke liye hai.
  }
}